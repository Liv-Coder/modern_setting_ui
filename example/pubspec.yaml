name: modern_setting_ui_example
description: Example app demonstrating the Modern Settings UI package

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.0.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  modern_setting_ui:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
